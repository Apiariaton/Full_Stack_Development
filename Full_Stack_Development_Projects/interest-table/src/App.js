import React, {useState} from "react";
import Investments from "./components/Investments";

// To Do
// -Read existing code
// -Create a suitable folder structure, dividing into components
// -Plan this as a tree diagram of components
// -Map this tree diagram to App.js
// -Create handler functions for the buttons on this app
// -Devise states for the components
// -Create suitable conditional logic for each component
// -Style each component via CSS modules
// -Test the code


//Sort out problems with calculateHandler within the ReturnItemTable component

function App() {

  return (
    <div>
      <Investments></Investments>
      </div>
        



       
  );
}

export default App;
